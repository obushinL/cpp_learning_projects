# Сортировка со стратегией и снимком состояния

В этом примере используются два шаблонных паттерна:

1. **Strategy** — для выбора порядка сортировки.  
2. **Memento** — для сохранения и восстановления исходного состояния контейнера.



